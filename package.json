{
  "name": "certichain-proofchain",
  "version": "1.0.0",
  "description": "MVP blockchain para registro e verificacao publica de certificados",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "register:sepolia": "hardhat run scripts/registrar-demo.js --network sepolia",
    "verify:sepolia": "hardhat run scripts/verificar-demo.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.22.10"
  }
}
